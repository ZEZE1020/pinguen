# Development Dockerfile for Go backend with hot reload support
FROM golang:1.25-alpine

# Install air for hot reloading (optional, but useful for development)
RUN go install github.com/cosmtrek/air@latest

# Set the working directory
WORKDIR /app

# Copy go mod and sum files
COPY go.mod go.sum* ./

# Download all dependencies
RUN go mod download

# Copy the source code
COPY . .

# Expose the port
EXPOSE 8080

# Add healthcheck
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
  CMD wget --no-verbose --tries=1 --spider http://localhost:8080/status || exit 1

# Command to run the application with hot reload
CMD ["go", "run", "main.go", "middleware.go"]
